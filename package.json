{
  "name": "spectrasynq-workspace",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "tools/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web-main",
    "build": "npm run build --workspace=apps/web-main",
    "lint": "npm run lint --workspace=apps/web-main",
    "test:visual": "node tools/visual-regression/capture.js"
  },
  "devDependencies": {
    "playwright": "^1.57.0"
  }
}
